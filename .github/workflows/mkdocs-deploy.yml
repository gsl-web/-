<<<<<<< HEAD
name: Deploy MkDocs

on:
  push:
    branches: [ main ]  # 你的主分支名，看你仓库是 main 还是 master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material
          
      - name: Build and deploy
=======
name: Deploy MkDocs

on:
  push:
    branches: [ main ]  # 你的主分支名，看你仓库是 main 还是 master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material
          
      - name: Build and deploy
>>>>>>> 1fc9002309274a6ca2a64a2f87ccc54998ede36f
        run: mkdocs gh-deploy --force